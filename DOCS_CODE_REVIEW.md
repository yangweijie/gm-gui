# 多代理代码审查系统

本项目包含一个全面的多代理代码审查系统，可对代码进行安全性、性能、质量和架构方面的自动化分析。

## 功能特性

- **安全审查**：检测代码中潜在的安全漏洞
- **性能审查**：识别性能瓶颈和优化机会
- **质量审查**：检查代码质量问题和最佳实践
- **架构审查**：确保架构一致性和正确的设计模式

## 组件说明

### 1. 配置文件
- 位置：`config/code-review-config.json`
- 包含每个审查类别的规则和模式
- 可自定义以添加新规则或修改现有规则

### 2. 审查脚本
- 位置：`bin/code-review.php`
- 运行代码审查的主入口点
- 在`logs/`目录中生成详细的JSON报告

### 3. CI集成脚本
- 位置：`bin/ci-review.sh`
- 用于将代码审查集成到CI/CD管道中的脚本
- 根据审查结果返回适当的退出代码

### 4. 预提交钩子
- 位置：`bin/pre-commit-hook.sh`（复制到`.git/hooks/pre-commit`）
- 在每次提交前自动运行代码审查
- 防止提交存在严重问题的代码

## 使用方法

### 手动运行代码审查
```bash
# 审查src目录
php bin/code-review.php src

# 审查特定目录
php bin/code-review.php src/Services
```

### CI集成
```bash
# 在CI管道中运行
./bin/ci-review.sh
```

### 预提交钩子
预提交钩子会自动安装，并在每次包含PHP文件更改的提交时运行。

## 审查类别

### 安全性
检测潜在的安全问题，如：
- 硬编码密钥或机密信息
- 不安全的文件操作
- 不当的错误处理

### 性能
识别性能相关问题：
- 低效的文件处理
- 次优的算法实现

### 质量
检查代码质量和可维护性：
- 代码复杂度
- 不完整的异常处理

### 架构
确保架构一致性：
- 服务依赖问题
- 分层架构违规

## 自定义配置

要添加新规则或修改现有规则，请编辑`config/code-review-config.json`文件。每个规则包含：
- `id`：规则的唯一标识符
- `name`：人类可读的名称
- `description`：规则的详细描述
- `severity`：问题严重性（high、medium、low）
- `patterns`：匹配有问题代码模式的正则表达式

## 报告生成

每次审查后，会在`logs/`目录中生成详细的JSON报告，命名模式为`code-review-report-YYYY-MM-DD-HH-MM-SS.json`。报告包含：
- 审查时间戳
- 每个类别的问题
- 每个问题的文件名和行号
- 匹配的模式
- 匹配的代码片段

## 当前发现的问题

根据最近的审查报告，系统识别出以下类型的问题：

### 安全问题（19个）
主要是不安全的文件操作，如直接使用`file_get_contents`和`file_put_contents`而没有适当的验证和错误处理。

### 质量问题（8个）
主要是代码复杂度问题，检测到一些复杂的代码模式。

## 改进建议

### 安全性改进
1. 对所有文件操作添加适当的输入验证
2. 实施更安全的文件处理方法
3. 添加适当的错误处理和日志记录

### 质量改进
1. 简化复杂的代码结构
2. 改进异常处理机制
3. 遵循更好的代码组织原则

## 集成到开发工作流

### Git钩子集成
系统通过预提交钩子自动集成到Git工作流中，确保在代码提交前进行审查。

### CI/CD集成
通过CI审查脚本，可以轻松将代码审查集成到任何CI/CD系统中。

### IDE集成
审查脚本可以作为IDE外部工具集成，提供实时反馈。